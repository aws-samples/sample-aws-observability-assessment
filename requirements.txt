# Python dependencies for the AWS Observability Assessment tool.
# Requires Python 3.12+ (the CodeBuild buildspec runs on python 3.12).
# The tool also requires the AWS CLI v2 to be installed and on PATH
# (it shells out to `aws ...`), which is not a pip package.
boto3>=1.34.0
